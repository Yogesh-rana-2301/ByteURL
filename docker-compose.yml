version: "3.8"

services:
  byteURL-app:
    build: .
    container_name: byteURL-app
    restart: always
    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - byteURL-redis
      - byteURL-postgres

  byteURL-postgres:
    image: postgres:16
    container_name: byteURL-postgres
    restart: always
    env_file: .env
    expose:
      - "5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d/

  byteURL-redis:
    image: redis:alpine
    container_name: byteURL-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "1"

volumes:
  pgdata:
  redis_data:
